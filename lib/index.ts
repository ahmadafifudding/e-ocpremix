export function calculateArea(length: number, width: number) {
  return length * width
}

export function calculateTonnage(area: number, depth: number, density: number) {
  return area * depth * density
}
